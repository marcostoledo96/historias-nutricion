# Configuración rápida Supabase

1) Crea un proyecto en https://supabase.com (plan gratuito)
2) En Settings > Database > Connection info, copia la cadena de conexión (URI)
3) En este archivo `.env` pega tu `DATABASE_URL` abajo y ajusta `NODE_ENV` a production:

DATABASE_URL=
NODE_ENV=production
SESSION_SECRET=un_secret_seguro

# Si te conectas desde local y hay problemas con certificados,
# desactiva SSL temporalmente (no recomendado en producción):
# NO_SSL=true
